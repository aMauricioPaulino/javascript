const nota = 8;
const bomComportamento = false;


if (nota >= 7 && bomComportamento)
    console.log("PARABÉNS");

if (nota < 7 || !bomComportamento)
    console.log("UMA PENA");

console.log("FIM");