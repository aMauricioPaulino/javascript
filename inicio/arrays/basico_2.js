const numeros = [1, 2, 3];

console.log(typeof numeros);
console.log(numeros);

numeros.push(4); //PUSH serve para adicionar valores dentro do ARRAY.
numeros.push(10);
numeros.push(6.5);

console.log(numeros);
console.log(numeros.length); // Para saber quantos valores tem dentro do ARRAY.

//Obs: ARRAY é um objeto, e um objeto é como se fosse uma pasta que armazena coisas.
