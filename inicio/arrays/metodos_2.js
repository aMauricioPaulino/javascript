const numeros = [1, 2, 3, 4, 5];

numeros.splice(0, 2); //Serve para retirar um valor do ARRAY. o primeiro valor sugerido quer dizer de onde vai começar e o segundo quantos valores vai excluir.
console.log(numeros);
numeros.pop(); //Serve para excluir o ultimo valor do ARRAY. Obs: Se jogar ele dentro do console ele exclui e mostra qual foi excluído.
console.log(numeros);
