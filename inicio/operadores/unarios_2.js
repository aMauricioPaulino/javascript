let a = 1;
let b = 2;


// console.log (+aa === b++); essa linha de comando é muito confusa para se entender por isso usamos as 3 linha de comando em baixa para o codigo ficar melhor expliocar.

a++;
console.log(a === b);
b++; // Não importa quanto mais linhas se criar o que importa é está claro.