// Operadores unários são aqueles que operam em cima de um único valor. Mais abaixo temos um exemplo:

a = + 1; // Esse dois são só para dar o exemplo que se faz com operador unário o mesmo que sem ele.
a += 1;
a++; // Esse é o operador unário.
++a; //Ele pode tbm ser colocado a esquerda da variável que irá fazer o mesmo efeito.
a--; // Mais um exemplo.
--a; //      '' .

console.log (a);

// OBS ADICIONAIS: 
// QUANDO O OPERADOR ESTÁ ASSIM: a + b significa que ele está (INFIX)
// QUANDO O OPERADOR ESTÁ ASSIM: a++   significa que ele esta (POSTFIX)
// QUANDO O OPERADOR ESTÁ ASSIM: ++a   significa que ele esta (PREFIX)